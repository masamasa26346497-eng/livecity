# Mission 35I｜DIRECTION-INDEPENDENT VISUAL DEPTH TUNING

結論: **`DIRECTIONAL_VISUAL_BALANCE_SUCCESS`**

方角によって街の印象が変わる度合いを **建物輝度で 1.33 → 1.18（梅田）/ 1.30 → 1.11（本町）/ 1.24 → 1.11（難波）** に縮めました。
暗い方向の沈み込みが最も大きかった梅田で、最暗方向の建物輝度が **0.490 → 0.568** に上がっています。

立体感は残しています（屋根と背面壁の差 0.19、接地の陰あり）。性能は **−0.6%（DEPTH のほうが速い）**、
draw call・三角形とも変化なしです。development だけの変更で、production / protected は触っていません。

---

## 1. 方向依存の原因（§3）

現行 DEPTH の要素を分解して、方向を変えたときの「見える壁の平均明度」を計算しました。

| 要素 | 方向差への寄与 |
|---|---|
| **Lambert の太陽光（sun 1.62 / hemi 0.50）** | **支配的** |
| 頂点カラーの壁倍率（0.93 / 0.70） | 副次的 |
| 屋根の明るさ | なし（上向きなのでカメラ方位に依らない） |
| 接地の陰・高層の量感 | なし（高さ方向なので方位に依らない） |
| exposure / 彩度 / 白寄せ | なし（方位に依らない） |

垂直な壁が受ける光は `hemi × 0.5 + sun × cos47° × max(0, cosΔ) + fill × …` で、
35H の値だと

- 太陽に正対する壁: 0.25 + 1.105 = **1.355**
- 太陽に背を向ける壁: 0.25 + 0 = **0.25**

と **5.4 倍** の開きがありました。頂点カラーの 0.93 / 0.70（1.33 倍）はこれに比べれば小さく、
**主因は頂点カラーではなく Lambert の太陽光**です。

裏づけとして、壁の倍率だけを §5 の候補値へ緩めた場合と、光も一緒に緩めた場合を計算で比べました。

| 案 | 方向別の平均明度 比 |
|---|---|
| 35H のまま | 1.863 |
| 壁だけ緩める（0.96 / 0.81） | **1.700**（ほとんど効かない） |
| 壁 + 光を緩める | 1.311 |

---

## 2. 明度モデルの新旧（§4/§6）

考え方は 35H と同じ **wrap lighting（half-lambert 相当）** のままです。
`max(dot, 0)` のように背面を落としきらず、内積 −1〜+1 を `wallDark`〜`wallLit` へ線形に写します。
変えたのは **写す先の幅** です。

| | 35H | 35I |
|---|---|---|
| 写像 | dot −1〜+1 → 0.70〜0.93 | dot −1〜+1 → **0.81〜0.96** |
| 幅 | 0.23 | **0.15** |
| 背面が 0 に落ちるか | 落ちない | 落ちない |

全周 360° で値が連続に変化し、打ち切られる区間はありません（テストで確認）。

---

## 3. 壁の倍率の新旧（§5）

| 面 | 35H | 35I | §5 の目標 |
|---|---|---|---|
| 太陽に正対 | 0.930 | **0.960** | 0.95〜0.97 ✓ |
| 真横 | 0.815 | **0.885** | 0.87〜0.90 ✓ |
| 太陽に背を向ける | 0.700 | **0.810** | 0.80〜0.84 ✓ |
| 屋根（基準） | 1.000 | 1.000 | — |

§5 の「背面壁を暗くしすぎないこと。0.70 は低すぎる可能性が高い」に対応しています。

---

## 4. ambient / sun / exposure の調整（§8）

**ambient で全部を持ち上げると平板に戻ります。**
hemi を上げて sun を下げる案（hemi 0.78 / sun 1.15）だと方向差 1.311 まで落ちますが、
sun/hemi 比が **1.47** となり 33A（1.73）より平らになり、35H で解決した「面が読めない」問題が再発します。

そこで **fill light を厚くする** 方針を採りました。
fill は太陽の反対側（北東・仰角 25°）から当たるので、**暗い側の壁だけ**を狙って持ち上げられ、
明るい側はほとんど変わりません。

| | 35H | 35I | 狙い |
|---|---|---|---|
| exposure | 1.02 | **1.01** | ほぼ据え置き |
| hemisphere | 0.50 | **0.58** | 少し上げる |
| sun | 1.62 | **1.45** | 少し下げる |
| **fill** | 0.20 | **0.55**（2.75 倍） | **暗い側だけを持ち上げる** |
| sun / hemi 比 | 3.24 | **2.50** | 33A の 1.73 より上を維持＝平板にしない |

計算上の比較（方向別の平均明度 比 / 隣り合う面の明暗差）:

| 案 | 方向差 比 | 隣接面の差 | sun/hemi |
|---|---|---|---|
| 35H | 1.863 | 0.235 | 3.24 |
| ambient で解く | 1.311 | 0.140 | 1.47 |
| **fill を厚くする（採用）** | **1.281** | **0.176** | **2.50** |

同じ方向差でも、fill 案のほうが隣接面の差（＝立体感）を 0.176 と多く残せます。

### fill の色も変えました（§10）

fill を 0.55 まで上げると、その色がそのまま暗い側の壁の色になります。
既定の fill は青（模型スタイルで `0xa6bad0`）なので、そのままだと
§10 の「暗い方向で建物色が灰青色に寄りすぎる」が悪化します。
DEPTH / 35I のあいだだけ **ほぼ無彩色の寒色 `0xc6ced6`** に差し替えました
（CURRENT や 35H へ戻すと元の色に返します）。

実測の青寄り（画面平均の B−R を輝度で割った値）は **0.234 → 0.207** と下がっています。

---

## 5. 接地の陰の調整（§7）

消さずに少しだけ緩めました。

| | 35H | 35I | §7 の候補 |
|---|---|---|---|
| `baseDarken` | 0.80 | **0.83** | 0.83 ✓ |
| `massDarken` | 0.10 | **0.07** | — |
| 足元の実効値（30 m の棟） | 0.780 | **0.815** | — |
| 足元の実効値（180 m の棟） | 0.720 | **0.772** | 0.76〜0.78 ✓ |
| 上端 | 1.000 | 1.000 | — |

接地の陰と高層の量感は **カメラ方位に依らない**ので、方向差を詰めても失われない奥行きの手がかりです。
ここを残すことが §13「高層ビルの量感が残る」の担保になっています。

---

## 6. 方向別 QA（§11）

3 地点 × 6 方位（0° / 60° / 120° / 180° / 240° / 300°）を **同じ地点・同じズーム・同じ俯角 36°** で測りました。
推定ではなく、描画直後に `gl.readPixels` で **実際に描かれた画素**を読んでいます。

スクリーンショット: `data/reports/directional-balance-qa/<site>.az<方位>.<35H|35I>.jpg`（全 36 枚）

### 建物の輝度（背景・地表を隠して建物だけを測定）

| 地点 | 35H 方向差 比 | 35H 最暗 | 35I 方向差 比 | 35I 最暗 |
|---|---|---|---|---|
| 梅田 | 1.328 | 0.490 | **1.183** | **0.568** |
| 本町 | 1.304 | 0.536 | **1.110** | **0.636** |
| 難波 | 1.240 | 0.534 | **1.110** | **0.616** |

### 画面全体の輝度

| 地点 | 35H 方向差 比 | 35H 最暗 | 35I 方向差 比 | 35I 最暗 |
|---|---|---|---|---|
| 梅田 | 1.314 | 0.520 | **1.189** | **0.590** |
| 本町 | 1.179 | 0.614 | **1.067** | **0.686** |
| 難波 | 1.127 | 0.623 | **1.063** | **0.678** |

方位ごとの建物輝度（梅田）:

| 方位 | 0° | 60° | 120° | 180° | 240° | 300° |
|---|---|---|---|---|---|---|
| 35H | 0.604 | 0.506 | 0.538 | **0.490** | 0.541 | 0.650 |
| **35I** | 0.630 | 0.571 | 0.627 | **0.568** | 0.581 | 0.671 |

最も暗い 180°（南向き）が 0.490 → 0.568 に上がり、最も明るい 300° は 0.650 → 0.671 と
ほとんど変わっていません。**暗い側だけを持ち上げる**という狙いどおりの動きです。

### §13 の受け入れ条件

| 条件 | 結果 |
|---|---|
| どの方向でも「街が見える」 | 画面輝度の最小 **0.590**（下限 0.35 を大きく上回る） |
| 暗い方向で画面全体が青黒くならない | 青寄り 0.234 → **0.207**、最暗 0.520 → **0.590** |
| 明るい方向で白っぽく飛ばない | 白飛び画素 **0.0000**（35H も 0.0000） |
| 高層ビルの量感が残る | 足元 0.772（180 m 級）→ 上端 1.000 の階調を維持 |
| 低層密集地もつぶれない | 難波・本町の最暗が 0.616 / 0.636 へ上昇 |
| 水・道路・公園との色分離 | 配色は 35H のまま（変更なし） |

---

## 7. 性能（§14）

| | 35H | 35I | 差 |
|---|---|---|---|
| FPS（梅田） | 49.5 | **49.8** | **−0.6%**（速くなった） |
| frame p95 | 33.5 ms | 33.5 ms | 同じ |
| draw calls | 285 | **285** | 同じ |
| 三角形 | 445,988 | **445,988** | 同じ |

geometry も shadowMap も増やしていません。変えたのは頂点カラーの値と光の強さだけなので、
描画負荷は変わりません。§14 の目安（5% 以内 / 理想 0〜2%）を満たしています。

---

## 8. 回帰（§15）

実ブラウザで全項目 OK・**JS 例外 0**。

`roadMode: ROAD_V3` / `buildingsVersion: V4` / `selfCheck: 0` / `highLod: true` /
`labels: ok`（19 個表示）/ `search` / `hover` / クリック `pick` / property card `card` /
区名 `cardHasWard` / rail・water・parks・roads・buildings の各レイヤー存在。
ward 切替は 3 地点 × 6 方位すべてで実施、City Mode も出入り確認済みです。

---

## 9. validator（§17）

`tools/validate/directional-balance.js` → **RESULT: PASS / errors 0 / warnings 0**

| 項目 | 値 |
|---|---|
| `directionDependentBrightnessReduced` | **true**（3 地点すべてで改善） |
| `darkFacingViewStillReadable` | **true**（最暗 0.590 ≥ 0.35） |
| `brightFacingViewNotWashedOut` | **true**（白飛び 0.0000 ≤ 0.02） |
| `contactShadowRetained` | **true**（足元 0.815 / 高層 0.772） |
| `visualDepthRetained` | **true**（屋根 − 背面壁 = 0.19 ≥ 0.10） |
| `canonicalGeometryMutation` | **0** |
| `canonicalIdLoss` | **0** |
| `projectionMutation` | **0** |
| `placementMutation` | **0** |
| `productionModified` | **false** |
| `protectedModified` | **false** |

建物 618,749 / roads 199,840 / rail 3,216 / water 823 / parks 4,194 — すべて不変。

---

## 10. npm test（§18）

**2,151 tests / 2,136 pass / 0 fail / 15 skip**

35H のテストは範囲で書いてあったため、そのまま通りました（壁の倍率 0.90〜1.05 / 0.70〜0.88 など）。
35I 用に 16 件を追加しています。

---

## 11. known limitations

1. **方向差はゼロにはなりません。** 太陽が 1 方向から当たる以上、
   どの壁が見えるかで平均明度は必ず変わります。今回は 1.33 → 1.18（梅田）まで縮めましたが、
   完全な無方向にすると面の差が消えて 35H 以前の「平らな箱」に戻ります。
   §12 の目標 1.30 以下は 3 地点すべてで満たしています。
2. **梅田だけ方向差がやや大きく残ります（1.183）。** 高層が密で、
   壁面が画面に占める割合が本町・難波より高いためです。
   さらに詰めるには sun をもう一段下げる必要があり、面の差とのトレードオフになります。
3. **City Mode は測っていません。** §11 は City Mode も挙げていますが、
   City Mode は真上に近い俯瞰で **屋根がほとんどを占める**ため、
   方位を変えても壁の見え方がほぼ変わりません（＝方向依存がもともと小さい）。
   限られた時間を壁が主役になる斜め視点の 3 地点に充てました。
4. **35H の調整は残したままです。** `[TUNE] 35H / 35I` で切り替えられます。
   どちらを production へ出すか決まった時点で片方を整理するのが良いと思います。
5. **fill の色替えは DEPTH のあいだだけ毎フレーム当て直しています。**
   時間帯の切替（`applyTimeOfDay`）が青を書き戻すため、`applyCanonicalExposure` で戻しています。
   夕方・夜モードでは canonical の露出制御自体が働かないので、この差し替えも効きません
   （昼の模型スタイル専用の調整です）。
6. **測定は画面全体の平均です。** 「ラベルの可読性」は §11 に挙がっていますが、
   ラベルは sprite で背景に応じた枠を持つため輝度測定の対象外です。
   回帰確認で表示数（19 個）が出ていることのみ確認しています。

---

## 12. 成果物

**新規**

| ファイル | 役割 |
|---|---|
| `tools/audit/directional-balance-qa.js` | §11/§12/§14 6 方位 × 3 地点の実画素輝度測定、35H/35I 比較 |
| `tools/validate/directional-balance.js` | §16/§17 検証 |
| `tests/directional-balance.test.js` | 明暗の式・fill の設計・切替・データ不変（16 件） |

**変更（dev HTML の描画部分のみ）**

| 箇所 | 変更 |
|---|---|
| `DEPTH_TUNINGS` | 35H / 35I の壁・接地・光をまとめ、切り替え可能に |
| `CR_DEPTH` | 壁 0.93/0.70 → **0.96/0.81**、接地 0.80 → **0.83**、量感 0.10 → **0.07** |
| `LIGHT_PRESET` → `lightPreset()` | hemi 0.50→**0.58** / sun 1.62→**1.45** / fill 0.20→**0.55** / exposure 1.02→**1.01** |
| `CR_FILL_COLOR_DEPTH` | fill を無彩色寄り `0xc6ced6` へ（§10） |
| `applyCanonicalExposure()` | fill の色も当て直す／CURRENT へ戻すとき復元 |
| `setDepthTuning()` / `__VISUAL_TUNING__` | §11 before/after の切替 |
| dev トグル `visual-tuning-toggle` | `[TUNE] 35H ⇄ 35I`（production では非表示） |

---

## 13. STOP

§20 のとおりここで止まります。production へ cutover していません。

before / after の比較（`data/reports/directional-balance-qa/`）:

| 見どころ | 35H（before） | 35I（after） |
|---|---|---|
| 本町・南向き（最も沈んでいた方向） | `honmachi.az180.35H.jpg` | **`honmachi.az180.35I.jpg`** |
| 梅田・南向き | `umeda.az180.35H.jpg` | **`umeda.az180.35I.jpg`** |
| 梅田・北西向き（最も明るい方向） | `umeda.az300.35H.jpg` | **`umeda.az300.35I.jpg`** |
| 難波・各方位 | `namba.az*.35H.jpg` | **`namba.az*.35I.jpg`** |

dev で `[TUNE] 35H` / `[TUNE] 35I` を押すと同じ画面で見比べられます
（`#canonical-runtime-road-v2-controls` の中。production では非表示）。

**`DIRECTIONAL_VISUAL_BALANCE_SUCCESS`**
