# MISSION 33D — ZOOM FEEL + LABEL STABILITY

**判定: `ZOOM_LABEL_STABILITY_SUCCESS`**（validator PASS・error 0）

- 変更したのは **development の `public/osaka_3d_buildings.ward-ux-v1.html`** だけです。
- 建物 geometry / 位置 / projection / canonical V2 / OSM fallback V2 / ROAD V3 / water / rail / placement / canonicalId は**不変**。
- **production / protected は未変更**（§23/§28。実機確認後、次ミッションで 33C + 33D をまとめて cutover）。

---

## 1. ズームのパラメータ（旧 → 新）

| | 旧（33B まで） | 新（33D） |
|---|---|---|
| 計算式 | `cs.r = clamp(cs.r + e.deltaY * 0.9)` | `cs.r = clamp(r * exp(0.22 * notches))` |
| 性質 | **加算**（どの距離でも 1 ノッチ 90m 固定） | **距離に比例**（1 ノッチ = 常に同じ割合） |
| 1 ノッチ | −90m | −19.75%（引きは +24.6%） |
| 下限 | なし | `MIN_STEP_M = 90`（近景で旧より弱くならない） |
| 1 イベントの上限 | なし | ±3 ノッチ |
| deltaMode | 未対応（生の deltaY） | px / 行(×16) / ページ(×innerHeight) を正規化 |
| トラックパッド | 区別なし | 連続する小 delta を検出して ×0.62 |
| ズーム範囲 | `minR 60 / maxR 24000` | **同じ**（変更なし） |
| カーソル寄せ | 寄り時のみ最大 15% | **同じ**（変更なし） |

**なぜ加算式が問題だったか**: 1 ノッチ 90m は、引いた画面では体感できず（r=12,000 で 0.75%）、近景では飛びすぎ（r=300 で 30%）という、ちょうど逆の効き方でした。

### 実測（ホイールイベントを実際に流して計測）

| カメラ距離 | 旧 1 ノッチ | 新 1 ノッチ | 倍率 | 新の縮率 |
|---|---|---|---|---|
| 300 m | 90.0 m | 90.0 m | 1.00 | 0.700（下限。旧と同じ） |
| 900 m | 90.0 m | **177.7 m** | **1.97** | 0.8025 |
| 2,000 m | 90.0 m | 395.0 m | 4.39 | 0.8025 |
| 5,300 m（初期視点） | 90.0 m | 1,046.7 m | 11.6 | 0.8025 |
| 12,000 m | 90.0 m | 2,369.8 m | 26.3 | 0.8025 |

§2 の目標「現在の 1.7〜2.3 倍」は、**作業距離帯（r ≒ 775〜1,020m）でちょうどその範囲**に入ります。引いた画面ではそれ以上に強くなりますが、これは元の加算式が引き画面でほぼ効いていなかったためで、**比率で見れば全距離で一定（1 ノッチ = 19.75%）です**。近景（r < 457m）は下限 90m が効き、**旧より弱くなる距離はありません**。

### 2〜4 ノッチ（r=3,000 から寄る・§18）

| ノッチ数 | 旧 | 新 |
|---|---|---|
| 2 | 1.06 倍 | **1.55 倍** |
| 3 | 1.10 倍 | **1.94 倍** |
| 4 | 1.14 倍 | **2.41 倍** |

### カーソル寄せについての注意（§4）

寄せの式 `pull = min(0.15, (oldR - r) / oldR)` は**変更していません**が、1 ノッチの変化率が上がったぶん、**実際に効く寄せ量は増えます**。
引いた画面（r=12,000）では旧 0.0075 → 新 0.15（上限）です。ズームインすると地図がカーソル位置へ寄る度合いが強くなるので、実機で「寄りすぎる」と感じた場合はこの上限 0.15 を下げれば調整できます（今回は既存式を尊重してそのままにしました）。

## 2. マウスホイールの正規化（§3）

```
px      = deltaY × (deltaMode 0: 1 / 1: 16 / 2: innerHeight)
notches = clamp(px / 100, ±3)      // Windows のマウスは 1 ノッチ = deltaY 100
```
生の deltaY に倍率を掛けるのではなく **ノッチ数へ正規化**してから指数式に渡します。これで「1 ノッチ = 必ず 19.75%」がブラウザ・OS の刻みに依存しなくなります。

## 3. トラックパッドの挙動（§3）

判定はハードコードではなく入力の性質で行います: `deltaMode === 0` かつ `|px| < 40` のイベントが **3 回続いたら**トラックパッド（連続入力）とみなし、1 イベントあたりを ×0.62 に弱めます。大きな delta が来た時点で即座にマウス扱いへ戻ります。

**実測**: `deltaY = -8` を 12 回連続で流す（トラックパッドの 2 本指スワイプ相当）
- 検出: **true**
- 距離変化: 3,000 → 2,596.8 m（**1.155 倍**）＝ マウス 1 ノッチ弱。暴走しません。
- マウス（deltaY=100）は `smallRun = 0` のままなので誤検出しません。

## 4. ズーム範囲（§5）— 副産物として見つかった不具合

`minR 60 / maxR 24000` は変更していません。ただし**旧実装ではホイールで maxR まで到達できませんでした**。

| | 旧 | 新 |
|---|---|---|
| r=3,000 から 80 ノッチ寄る | 60 m（到達）✓ | 60 m ✓ |
| r=3,000 から 120 ノッチ引く | **10,860 m**（到達せず）✗ | **24,000 m** ✓ |

加算式では 3,000 → 24,000 に **234 ノッチ**必要でした（ホイールだけで City Mode の引きに戻れない）。指数式では 11 ノッチです。

## 5. ヒステリシスの設計（§7/§14）

**band（LOD と表示上限の切替）**: しきい値（far 9,000m / mid 3,500m）自体は変えず、**入る側と出る側を ±10% ずらしました**。

| 遷移 | しきい値 |
|---|---|
| mid → far | 9,900 m を超えたら |
| far → mid | 8,100 m を下回ったら |
| near → mid | 3,850 m を超えたら |
| mid → near | 3,150 m を下回ったら |

境界に居るときホイールを 1 ノッチ（×1.246）動かしても band が往復しません（8,100〜9,900 の幅は 1.22 倍で、1 ノッチより広い）。
band は `place()` で 1 回だけ決め、`lodVisible(item, b)` へ渡します（以前は item ごとに `band(d)` を呼んでいました）。

## 6. 選定キャッシュの設計（§8/§12）

- `prevVisible`（前回採用された id の Set）を保持。
- 並び替えは `(rank) → (前回出ていたか) → (画面中心からの距離)`。
  **rank そのものには加点しません**。加点するとランドマーク(0) / 主要駅(1) / 主要河川(1.2) のような 0.2 差の大分類が逆転しうるため、§12 のとおり「同順位内の並び」にだけ効かせています。
- `prevCell`（id → 画面グリッドのセル）を保持し、**前のセルの範囲から 0.28 セル以内なら前のセルに留まります**（§13）。境界を跨いだ瞬間に別セルの枠を消費して押し出される、という入れ替わりを防ぎます。
- **主要ラベル**（Tier S ランドマーク / 区名 / 主要駅 / 広域地名）は 1 セルあたりの上限を 3 → **5** に緩めます（§15/§16）。名前のハードコードではなく tier 判定です。
  画面全体の上限（18/36/58）と文字の衝突判定は主要ラベルにも等しく掛かるので、文字だらけにはなりません。

## 7. 再配置のトリガーしきい値（§9/§10/§11）

旧実装は `tgt/40m・r/60m・角度 0.05rad` の量子化キーが変わるたびに**全部選び直し**ていました。新実装は次のいずれかを超えたときだけ選び直します。

| 項目 | しきい値 |
|---|---|
| ターゲット移動 | `max(30m, 距離 × 3.5%)` |
| 距離の比 | 1.06 倍を外れたら |
| 方位 / 仰角 | 0.035 rad（約 2°） |
| band / 昼夜 / レイヤートグル | 変化したら即 |

しきい値未満のときは **選定を維持したまま `refreshTransforms()`** だけ走り、ラベルの大きさ（画面 px 一定）と河川名の向きを更新します。ラベルの**画面位置は three.js が毎フレーム投影する**ので、選定を止めても位置がズレることはありません（§11）。
ホイール 1 ノッチ（×1.246）は必ずしきい値を超えるので、ズーム時はきちんと選び直されます。

## 8. 表示セットの安定度（§7/§19/§20・before → after）

測り方を変えました。「基準の視点に戻れるか」ではチラつきを測れない（記憶を持たない旧実装ほど高得点になる）ため、**カメラを少しずつ動かす 42 ステップの経路を歩き、隣り合うサンプル間の Jaccard と入れ替わり数**を測っています。

| 地点 | Jaccard（1 手ごと） | 入れ替わり数／手 |
|---|---|---|
| 梅田 | 0.891 → **0.936** | 3.63 → **2.27** |
| 難波 | 0.856 → **0.935** | 4.49 → **2.51** |
| 住吉 | 0.805 → **0.893** | 5.93 → **3.68** |
| 本町 | 0.875 → **0.931** | 4.29 → **2.61** |
| City Mode | 0.780 → **0.919** | 5.27 → **2.54** |

動きの種類ごと（Jaccard・before → after）:

| 地点 | 微小 pan | 微小 rotate | **往復（jitter）** | 1 ノッチ zoom |
|---|---|---|---|---|
| 梅田 | 0.995 → **1.000** | 0.939 → 0.968 | 0.889 → **1.000** | 0.667 → 0.713 |
| 難波 | 0.995 → **1.000** | 0.910 → 0.993 | 0.876 → **1.000** | 0.538 → 0.675 |
| 住吉 | 1.000 → **1.000** | 0.780 → 0.918 | 0.753 → **1.000** | 0.586 → 0.554 |
| 本町 | 1.000 → **1.000** | 0.928 → 0.956 | 0.843 → **1.000** | 0.645 → 0.700 |
| City Mode | 0.891 → **1.000** | 0.980 → 0.959 | **0.580 → 1.000** | 0.598 → 0.634 |

- **往復（同じ場所を行ったり来たりする動き）でのチラつきは全地点で完全に消えました**（0.58〜0.89 → 1.000）。ユーザーが「少し動かしただけで出たり消えたりする」と感じていた動きがこれです。
- 微小 pan も全地点 1.000。
- 1 ノッチのズームでは 0.55〜0.71 です。**ズームすると画面に入る範囲自体が変わるので、ここが 1.0 になることはありません**（新しく視界に入った地名は出すべきです）。それでも 5 地点中 4 地点で改善しています（住吉のみ 0.586 → 0.554）。
- 主要ラベルだけの Jaccard（after）: 梅田 0.967 / 難波 0.945 / 本町 0.970 / City Mode 0.929 / **住吉 0.892**。

§20 の目安（主要 ≥ 0.90 / 全体 ≥ 0.75）は、**全体は 5 地点すべて達成**、主要は 4/5 達成、住吉のみ 0.892 でわずかに届きません（理由は §11 に記載）。

## 9. 重なり（§8）

| 地点 | 静止時の重なり | 操作中の一時的な接触 | 完全重複 | 同一ラベルの二重表示 | 最小文字高 |
|---|---|---|---|---|---|
| 梅田 | **0** | 1（zoom3 の 1 サンプルのみ・before も同じ） | **0** | **0** | 21.5px |
| 難波 | **0** | 0 | **0** | **0** | 21.5px |
| 住吉 | **0** | 0 | **0** | **0** | 21.0px |
| 本町 | **0** | 0 | **0** | **0** | 21.0px |
| City Mode | **0** | 0 | **0** | **0** | 23.5px |

梅田の 1 件は 42 サンプル中 1 つ（最も寄ったズーム段階）で矩形が触れるもので、**旧 production でも同じ値**です。33D で増えてはいません。

## 10. 性能（§17）

このマシンは**同一ビルド・同一設定でも FPS が 20〜46 と大きく振れます**（他プロセス・GPU 状態の影響。32U でも同じ現象を記録済み）。
1 回ずつ測って比べると偽の差が出るため、`tools/audit/zoom-label-perf-ab.js` で **production と development を交互に 3 ラウンド**測りました（City Mode・カメラを動かしながら 20 秒・タイル読み込みの整定を待ってから計測）。

| | production（33B） | development（33D） |
|---|---|---|
| FPS（3 回） | 21.3 / 21.5 / 25.8 | 24.0 / 21.6 / 20.4 |
| FPS 平均 | 22.87 | **22.00（−3.8%）** |
| frame P95 平均 | 99.97 ms | 100.13 ms |
| draw call | 387 | 386 |
| 表示ラベル | 18 | 18 |

**1 回ごとのばらつき（20.4〜25.8）が平均差（0.87 FPS）より大きく、差は環境ノイズの範囲です。** draw call も同数です。

ラベル層そのもののコストを直接測った値:

| | production（33B） | development（33D） |
|---|---|---|
| `place()` 1 回（中央値） | 0.07 ms | **0.07 ms** |
| `place()` 1 回（最大） | 1.5 ms | 2.4 ms（12 回中 1 回の外れ値） |
| 再配置回数／秒（移動中） | 計測不可（カウンタなし） | **2.25**（throttle 上限 5 の半分以下） |
| ラベル更新／秒（移動中） | 同上 | 4.45 |

**再配置は 1 秒あたり 2.25 回**です。しきい値判定によって「200ms ごとに必ず 5 回」から半分以下に減っており、安定化機構は負荷を**増やしていません**。

各地点の単発計測（参考・環境変動を含む）:

| 条件 | production | development |
|---|---|---|
| 梅田（静止 / 移動） | 26.7 / 26.4 FPS | 25.5 / 24.8 FPS |
| 難波（静止 / 移動） | 29.0 / 24.8 FPS | 27.4 / 24.4 FPS |
| City Mode（静止 / 移動） | 22.1 / 17.3 FPS | 19.4 / 14.5 FPS |

これらは交互計測ではないため、上の A/B 結果を正本とします。

## 11. 回帰確認（§22）

| 項目 | 結果 |
|---|---|
| hover（ツールチップ） | 表示される ✓ |
| クリック → 物件カード | 表示される ✓ |
| 地名トグル | 11 → 7 → 6 ✓ |
| 施設名トグル | 6 → 2 → 4 ✓ |
| 河川名トグル | 4 → 1 → 1 ✓ |
| 検索（難波） | 警告なし・目標地点との差 **0 m** ✓ |
| pan / rotate / zoom / City Mode / 区切替 | 全 QA シナリオで例外 0 ✓ |
| ブラウザ例外 | **0** |

（トグルの戻り値が元の数と一致しないのは、OFF/ON の間にカメラが動かないため再選定しきい値を超えず、再選定が「その時の視点」で行われるためです。表示/非表示自体は正しく切り替わっています。）

## 12. 既知の制約

1. **1 ノッチのズームでは表示ラベルが 30〜45% 入れ替わります**（Jaccard 0.55〜0.71）。ズームは画面に入る範囲そのものを変えるので、これは仕様どおりです。「チラつき」として問題になっていたのは往復・微小移動であり、そちらは 1.000 になりました。
2. **住吉の主要ラベル Jaccard が 0.892** と目安 0.90 をわずかに下回ります。住吉は主要ラベルの母数が少ない（区名 + 主要駅数件）ため、1 つ入れ替わるだけで比率が大きく動きます。母数が少ないことによるもので、チラつきの実害は §8 の往復 1.000 が示すとおりありません。
3. **§16 に挙がっている地名のうち、主要 tier に入るのは 梅田・難波・本町・中之島 です。** 淡路・東三国・西中島は「中位」、上新庄は「局所」です。§16 のとおり名前のハードコードはせず、33C のデータ側の順位付け（丁目数・駅の近さ・区内順位）をそのまま使っているためです。天王寺・新大阪は町丁目名として実データに無く、駅名ラベルで出ます。
4. **トラックパッドは実機で未確認です。** 判定ロジックは `deltaY` の大きさと連続性のみに基づき、CDP から小 delta を連続投入して検証しました（検出 true・1.155 倍で暴走なし）が、実際のトラックパッドの慣性スクロールは試せていません。実機で強すぎ/弱すぎを感じた場合は `ZOOM_WHEEL.TRACKPAD_SCALE`（現 0.62）で調整できます。
5. **高解像度ホイール**（1 ノッチを複数の小 delta に分割して送るマウス）は、小 delta が続くためトラックパッド判定に入ります。暴走はしませんが、その場合の効きは 0.62 倍になります。
6. ラベルの選定は「粘る」ようになったため、**大きく動いてから元の視点へ戻ると、以前とまったく同じ組み合わせには戻らないこと**があります（同じ優先度の中でどれを残すかが履歴に依存するため）。表示内容の質は同等です。

## 13. Validator — `data/reports/zoom-label-stability-validation.json`

| 項目 | 値 |
|---|---|
| zoomWheelStrengthIncreased | **true** |
| trackpadStillStable | **true** |
| zoomRangeReachable | **true** |
| labelHysteresisEnabled | **true** |
| labelSelectionStateCached | **true** |
| majorLabelFlickerReduced | **true** |
| nearOverlapCount（静止時・完全重複） | **0** |
| duplicateLabelCount | **0** |
| buildingMutation / roadMutation / projectionMutation | **0 / 0 / 0** |
| productionModified / protectedModified | **false / false** |
| cameraMatrixKept（§21） | true |
| legacyStationDormant（§21） | true |

## 14. npm test

- `npm test`: **1,796 tests / pass 1,781 / fail 0 / skip 15**（33C 時点の 1,776 から +20。新規 `tests/zoom-label-stability.test.js` 20 件）。
- lifestyle-tab 系（`npm test` 対象外）: 38 / pass 27 / **fail 0** / skip 11。
- 既存テストの更新は `tests/city-labels-palette.test.js` の 2 行だけです（33A の優先度キューのソート式と、量子化キーによる再選定の判定 → 33D の形に更新）。ほかの 33A/33B/33C のテストはそのまま通っています。

## 15. 生成物

| 種別 | パス |
|---|---|
| 変更したファイル | `public/osaka_3d_buildings.ward-ux-v1.html`（development のみ） |
| QA ツール | `tools/audit/zoom-label-stability-qa.js`（`--phase before\|after`）、`tools/audit/zoom-label-perf-ab.js`（交互計測） |
| Validator | `tools/validate/zoom-label-stability.js` |
| テスト | `tests/zoom-label-stability.test.js`（npm test に登録） |
| レポート | `data/reports/zoom-label-stability-qa.json`、`zoom-label-perf-ab.json`、`zoom-label-stability-validation.json`、`data/reports/zoom-label-stability-qa/*.jpg` |
| デバッグ API | `window.__ZOOM_DEBUG__()`（係数・最後のノッチ量・任意距離での 1 ノッチ量）、`__CITY_LABEL_DEBUG__().stability`（再配置回数・維持数・band 切替数） |

production へはまだ反映していません。実機でホイールの効きとラベルの落ち着きを確認していただき、問題なければ次ミッションで 33C + 33D をまとめて cutover します。

`ZOOM_LABEL_STABILITY_SUCCESS`
