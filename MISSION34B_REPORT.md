# MISSION 34B — HIGH LOD VISUAL QUALITY

**判定: `HIGH_LOD_VISUAL_QUALITY_SUCCESS`**

geometry / 建物位置 / 投影は **1mm も変えていません**（§1 の照合結果は後述）。変更したのは material・lighting・camera preset・dev QA UI だけです。production / protected は未変更で、**cutover もしていません**。

---

## 1. 原因（なぜ LOD2/LOD3 の屋根が見えていなかったか）

変更前の画面を 5 地点で実測したところ、屋根が見えない理由は 2 つありました。

| 計測 | 変更前 | 意味 |
|---|---|---|
| 屋根／壁の**色**の明るさ比 | **0.880〜0.886** | 屋根の用途色を `ROOF_SHADE = 0.88` で **12% 暗く**していた |
| 屋根／壁の**受ける光**の比 | **0.590〜0.613** | 主光が東南東・仰角 33.9° だったため、屋根に当たる光が壁より **39% 少なかった** |

つまり「屋根を暗くする設定」と「屋根に光が当たらない角度」が重なって、せっかくの LOD2/LOD3 の段差・塔屋が**影も明暗差もないのっぺりした面**になっていました。

さらに、影の正射影カメラが**原点固定（±2600×±1300m）**だったため、本町・梅田・新大阪のように注視点が原点から数 km 離れる場所では**影が 1 つも落ちていません**でした。段差が影を落とさないので、屋根の凹凸が読み取れませんでした。

## 2. 直したこと（material / lighting / camera / QA のみ）

### 2-1. 屋根と壁の分け方（§4/§5/§16）

`ROOF_SHADE = 0.88`（暗くする）を廃止し、**壁と同じ色相のまま相対輝度を +9%・彩度を −3%** にしました。

最初は「HSL の明度 L を 1.09 倍」で実装しましたが、用途色ごとに輝度の上がり方が **+3.8%（学校）〜+13.0%（公共）**とばらつき、§4 の 6〜12% を外れることが計算で分かりました。そこで**輝度そのものを目標値にして必要な L を解く**方式へ変更しています（輝度は L に対して一次なので 2 点から傾きを出して一度で解ける。用途色は 10 種類なので結果は cache）。

| | 全 10 用途色での結果 |
|---|---|
| 相対輝度比（屋根 ÷ 壁） | **1.0900 〜 1.0900**（ばらつき < 0.001） |
| 色相のずれ | **0.00000°** |
| 彩度の低下 | **3.00%**（§4 の 0〜5% 内） |

白で塗りつぶしてはいません。彩度は 97% 残しており、用途色の見分けはそのままです。

### 2-2. 材質（§4）

`MeshLambertMaterial` → **`MeshStandardMaterial` + `flatShading`** へ。

| surface | roughness | metalness |
|---|---|---|
| roof | **0.76** | 0.03 |
| wall | **0.85** | 0.02 |
| closure | 0.84 | 0.02 |
| ground | 0.88 | 0.00 |
| other | 0.82 | 0.02 |

すべて §4 の roughness 0.72〜0.88 / metalness 0.00〜0.05 に収めています。`flatShading` は**面ごとの法線で陰影を付ける**ので、勾配や段の違う屋根面の境目がはっきり出ます（geometry は変えず、陰影だけで段差を見せる）。テクスチャ・環境マップは使っていないので、Google Earth 的な写実にはなりません。

### 2-3. 光（§7/§8/§9）

| | 変更前 | 変更後 |
|---|---|---|
| 主光 方位 | 116.6°（東南東） | **236.0°（南西〜西南西）** |
| 主光 仰角 | 33.9° | **47.0°** |
| 主光 位置 | `[2400, 1800, 1200]` | `[-2224, 2877, 1500]` |
| 補助光 | 西北西（290.6°） | **北東（56.0°）＝主光の対角** |
| hemi / exposure / sun / fill の強さ | 0.74 / 0.93 / 1.28 / 0.26 | **変更なし**（33A の `CR_STYLE` のまま） |

環境光も露出も上げていません。§9 のとおり、明るさではなく**光の向き**で立体感を出しています。補助光（0.26）は残してあるので影側は潰れません。

**影の正射影カメラを注視点へ追従**させました。光の向き（`SUN_OFFSET`）は変えず、光源位置と注視点だけを平行移動し、影の範囲は `cs.r × 1.3`（320〜2600m）に絞ります。これで「見ている場所に影が落ちる」ようになり、段差・塔屋が影を落とすようになりました。同じ 4096 の shadow map で解像度も上がります（本町で 5200×2600m → 2340×2340m）。

### 2-4. dev QA（§22/§13）

| UI | 動作 |
|---|---|
| `[LOD1 ONLY]` | 高 LOD を止めて LOD1 の箱だけにする（同一 camera で ON/OFF 比較） |
| `[HIGH LOD]` | 通常表示。用途色のまま。**形・影・光だけで差を見せる** |
| `[LOD DIFF]` | QA 専用。LOD1 灰 / LOD2 青 / LOD3 金 |
| `[ROOF QA]` | QA 専用。面の法線を色にする（上向き＝緑） |
| `[LOD VIEW]` | pitch 50° / fov 44° / 900m の斜め視点。ラベル密度も落とす |
| `[LOD 本町] [LOD 梅田] [LOD 中之島] [LOD 大阪城] [LOD 新大阪]` | 地点別 preset |

比較モードは material を差し替えるのではなく **color 属性を差し替える**実装にしてあります（material は 1 枚のままなので draw call は増えません）。LOD DIFF のあいだだけ LOD1 の箱を無彩色にし、通常表示では必ず用途色へ戻します。

## 3. 結果（同一 camera・5 地点の実測）

### 3-1. 屋根と壁が見分けられるか（§4）

| 地点 | 色の比（前 → 後） | 受光込みの比（前 → 後） |
|---|---|---|
| 本町 | 0.882 → **1.088** | 0.611 → **1.062** |
| 梅田 | 0.886 → **1.095** | 0.613 → **1.097** |
| 中之島 | 0.883 → **1.091** | 0.611 → **1.091** |
| 大阪城 | 0.885 → **1.095** | 0.613 → **1.066** |
| 新大阪 | 0.880 → **1.089** | 0.590 → **1.054** |

**「屋根のほうが暗い」から「屋根のほうが明るい」へ反転**しました。色の比は全地点 §4 の 1.06〜1.12 内です。

### 3-2. LOD1 との差が画面に出ているか（§3）

同一 camera の `HIGH LOD` と `LOD1 ONLY` で、画面の**局所コントラスト**（隣り合う画素の明るさの差＝面の切り替わりの多さ）を比べました。

| 地点 | LOD1 ONLY | HIGH LOD | 差 |
|---|---|---|---|
| 本町 | 0.02007 | 0.02994 | **+49.2%** |
| 梅田 | 0.02542 | 0.03146 | **+23.8%** |
| 中之島 | 0.01718 | 0.02472 | **+43.9%** |
| 大阪城 | 0.00783 | 0.00932 | **+19.0%** |
| 新大阪 | 0.01322 | 0.02205 | **+66.8%** |

変更前の同じ指標は本町 **+5.0%** / 梅田 +12.0% / 中之島 +24.9% / 大阪城 +9.6% / 新大阪 +48.9% でした。**本町で +5% → +49%** と、いちばん効いてほしかった密集市街地で大きく改善しています。

### 3-3. 屋根に段差・塔屋があるか（§23-§27）

`[LOD VIEW]` の視点で、見えている高 LOD 建物の屋根面を高さ 1m 刻みで束ね、面積 3% 以上の高さを「段」として数えました。

| 地点 | 屋根を持つ棟 | **段差のある屋根** | 平均の段数 | 屋根高さの広がり p90 |
|---|---|---|---|---|
| 本町 | 2,841 | **76.1%** | 2.74 | 17.06 m |
| 梅田 | 1,221 | **71.4%** | 2.63 | 15.52 m |
| 中之島 | 1,840 | **71.8%** | 2.65 | 15.79 m |
| 大阪城 | 1,267 | **60.3%** | 2.32 | 13.65 m |
| 新大阪 | 1,326 | **61.8%** | 2.22 | 16.69 m |

**本町は 7 割以上の棟が「箱の上面 1 枚」ではありません。**平均 2.74 段あり、屋根の高さは p90 で 17m 開いています。これが `[LOD1 ONLY]` では全棟 1 段になります。

### 3-4. 地点ごとの受入条件

| 地点 | 条件 | 結果 |
|---|---|---|
| 本町 | 塔屋・段差・複数段の屋根が見えるか | **76.1% の棟が複数段**（screenshot `honmachi-high-after.jpg` / `honmachi-roofqa-after.jpg`） |
| 梅田 | 屋根のセットバック・屋上設備 | 大阪駅の屋上設備・ノースゲートビルディングのセットバック・HEP FIVE の段状屋根が視認できる（`umeda-high-after.jpg`） |
| 中之島 | 川のほうへずれていないか | **建物は岸に収まっている**。geometry は未変更で、描画中の頂点は 34A のデータと完全一致（§4 参照） |
| 大阪城 | LandmarkHD 優先が保たれているか | **LandmarkHD が持つ棟を高 LOD が描いた件数 0**。承認済みモデル > LOD3 > LOD2 > LOD1 の順序は保持 |
| 新大阪 | 屋根が線路へ張り出していないか | 線路上に屋根は出ていない（`shinosaka-high-after.jpg`）。footprint から 2m 超はみ出す棟は **1,326 棟中 4 棟（0.30%）** |

> **正直な補足（新大阪・大阪城のはみ出し）**: 屋根の頂点が canonical footprint の外接円から最大 **11.37m**（新大阪・footprint 半径 30.8m の棟）/ **9.06m**（大阪城）出ている棟がそれぞれ 1 件あります。これは **34A が抽出した geometry の性質**であり（34A は `maxBboxAreaRatio: 4` まで許容）、34B では 1mm も変えていません。該当は各地点 1,300 棟中 3〜4 棟で、線路上への張り出しではありません。

## 4. geometry / 位置 / 投影を変えていないことの確認

| 確認 | 方法 | 結果 |
|---|---|---|
| **描画中の頂点 = 34A のデータ** | ブラウザで高 LOD メッシュの指紋（頂点数・座標の総和・bbox・index 数・part 数）を取り、タイル JSON から同じ手順で組み直したものと突き合わせ | **352 メッシュ中 一致 352 / 不一致 0** |
| 高 LOD タイルが書き換えられていないか | ファイル数・総バイト・更新時刻を 34A のビルド記録と照合 | **87 ファイル / 46,389,607 バイト 一致**・更新なし |
| 頂点を積む経路 | `acc.pos.push(p.positions[i], …)` のまま（34B で触ったのは `acc.col` だけ） | 不変 |
| 各地点で描かれる棟数 | 34A cutover 前チェックの実測値と照合 | 本町 2,842 / 梅田 1,221 / 中之島 1,841 / 大阪城 1,268 / 新大阪 1,333 — **全一致** |
| canonical V2N の建物数 | manifest | **600,764**（不変） |
| 投影 | `config/areas/osaka-city.json` | local-equirectangular / 34.604208 / 135.52502 / 111320 — 不変 |
| Zone VII | コードのみを検査 | **使用なし** |

> 照合で 2 件だけ不一致が出たときがありましたが、原因は**大阪城の LandmarkHD が持っている 1 棟を高 LOD 側が描かない（正しい挙動）**のに、再現側がその棟を数えていたためでした。除外して再照合し、不一致 0 です。

> **できなかったこと**: 変更前後で「実際に描いている頂点」を直接突き合わせる指紋は、**変更前の計測にこの指紋を入れていなかった**ため取れていません（`comparedMeshes = 0`）。代わりに上表の「タイルデータとの直接照合」で担保しています。

## 5. 性能（§28・4 地点 × 30 秒）

| 地点 | LOD1 ONLY | HIGH LOD | 差 |
|---|---|---|---|
| 本町 | 49.6 fps | **41.9 fps** | −15.5% |
| 梅田 | 51.8 fps | **50.1 fps** | −3.3% |
| 中之島 | 58.4 fps | **48.9 fps** | −16.3% |
| 新大阪 | 59.9 fps | **59.5 fps** | −0.7% |

**34B の変更そのものにコストがあります。** 影を注視点へ追従させた結果、これまで「影の描画対象が frustum 内にほぼ無い」ぶん只だった shadow pass に実コストが乗りました。

| 地点（HIGH LOD） | 34B 前 | 影追従を入れた直後 | 影マップ間引き後（最終） |
|---|---|---|---|
| 本町 | 51.9 fps | 38.8 fps | **41.9 fps** |
| 梅田 | 57.1 fps | 45.0 fps | **50.1 fps** |
| 中之島 | 59.7 fps | 44.8 fps | **48.9 fps** |
| 新大阪 | 59.8 fps | 56.2 fps | **59.5 fps** |

対策として **影マップの更新を 3 フレームに 1 回**へ間引きました（`renderer.shadowMap.autoUpdate = false` ＋ 描画ループで `needsUpdate`）。影は動かない geometry に落ちるだけなので、1〜2 フレーム遅れても見た目には出ません。

それでも 34B 前より本町で約 10 fps 下がっています。**これは「影がまったく落ちていなかった状態」から「影が落ちる状態」への変化の代価**で、屋根の段差が読めるようになった主因でもあります。負荷が問題になる場合は `window.__SUN_SHADOW_FOLLOW__(false)` で追従だけ切れるようにしてあります（`SHADOW_THROTTLE.every` も調整可）。

## 6. 変えていないもの

| | 結果 |
|---|---|
| 用途色 palette（33A/33B） | `CR_USAGE_COLOR` / `CR_VIVID {sat:1.24, light:1.03}` / `CR_USAGE_WHITEN` すべて不変 |
| 高 LOD の色の出所 | `CanonicalRuntime.buildingMaterial(cat, band)`（LOD1 と同じ material）のまま |
| 全建物への黒 outline | **入れていない**（§11）。dev の edge overlay も追加していない |
| 通常の高 LOD 表示での透明化 | **なし**（§12。透明は LOD カバレッジ QA の面だけ） |
| production HTML | **未変更**（ビルド記録の sha256 と一致） |
| protected HTML | **未変更**（baseline hash と一致） |

## 7. Validator / テスト

| | 結果 |
|---|---|
| validator | **PASS `HIGH_LOD_VISUAL_QUALITY_SUCCESS`**（errors 0 / warnings 1） |
| `geometryMutation` | **0** |
| `positionMutation` | **0** |
| `projectionMutation` | **0** |
| `highLodRoofReadable` | **true** |
| `roofWallVisualSeparation` | **true** |
| `lodDiffQaAvailable` | **true** |
| `lodCameraPresetsAvailable` | **true** |
| `productionModified` / `protectedModified` | **false / false** |
| `npm test` | **1,865 / pass 1,850 / fail 0 / skip 15** |
| lifestyle-tab | 38 / pass 27 / **fail 0** / skip 11 |

warning は §3-4 に書いた「34A 由来のはみ出し 5m 超が 2 地点に各 1 棟」のみです。

### 既存テストの更新（4 件）

| テスト | 理由 |
|---|---|
| `max-plateau-lod.test.js` §16/§19 | material が Lambert → Standard+flatShading、屋根補正が `ROOF_SHADE` → `tintRoof` になったため、検査文字列を更新 |
| `canonical-runtime-cutover.test.js` FIX5 / FIX8B | LOD DIFF の灰色分岐を入れたため `meshFromPositions(...)` の行が変わった。**「用途色 × band で material を共有する」という主旨は維持** |
| `landmark-hd-poc.test.js` §2 | 検査範囲の終端が `const CanonicalRuntime` で、あいだに入った 34A/34B のレイヤーまで巻き込んでいた。LandmarkHDLayer の直後で切るよう修正（過去にも同種の「固定終端が古びる」問題あり） |
| `mission11-landmarks.test.js` | dev QA の camera preset（`LOD_VIEW_SITES`）が地点名を持つため。**この preset がランドマークのレジストリに一切繋がっていないこと**を新たに assert したうえで、名前の検査対象から外した |

> 最後の 1 件は「ランドマーク名を HTML に書かない」というガードの**例外を 1 つ作った**ことになります。ミッションが `[LOD 大阪城]` というボタン名を明示していたため、地点名の表示自体は避けられませんでした。ガードの本来の目的（表示されるランドマーク名がレジストリ以外から来ないこと）は維持しています。判断が違う場合はお知らせください。

## 8. スクリーンショット（40 枚）

`data/reports/high-lod-visual-qa/` 配下。5 地点 × 以下の 8 種類。

| 接尾辞 | 内容 |
|---|---|
| `-high-before` / `-high-after` | 同一 camera の HIGH LOD（**変更前 / 変更後**） |
| `-lod1-before` / `-lod1-after` | 同一 camera の LOD1 ONLY |
| `-diff-before` / `-diff-after` | LOD DIFF（変更前はモードが無いため HIGH LOD と同じ） |
| `-preset-after` | `[LOD VIEW]` の preset そのままの視点（UI 非表示） |
| `-roofqa-after` | ROOF QA（法線可視化・UI 非表示） |

まず見ていただきたいのは **`honmachi-high-before.jpg` と `honmachi-high-after.jpg` の比較**です。

## 9. 成果物

| 種別 | パス |
|---|---|
| 変更した HTML | `public/osaka_3d_buildings.ward-ux-v1.html`（**development のみ**） |
| 見え方の計測 | `tools/audit/high-lod-visual-qa.js` → `data/reports/high-lod-visual-qa.json` |
| 屋根構造の計測 | `tools/audit/high-lod-roof-structure-qa.js` → `data/reports/high-lod-roof-structure-qa.json` |
| validator | `tools/validate/high-lod-visual-quality.js` → `data/reports/high-lod-visual-quality-validation.json` |
| テスト | `tests/high-lod-visual-quality.test.js`（32 件・`npm test` に登録済み） |
| 更新したテスト | `max-plateau-lod` / `canonical-runtime-cutover` / `landmark-hd-poc` / `mission11-landmarks` |

## 10. やらなかったこと

| 項目 | 理由 |
|---|---|
| very weak AO | 仕様上 optional。屋根の可読性は影と面ごとの陰影で足りており、LOD1 の箱（平坦な単色）との見え方の差が広がるのを避けた |
| dev 専用 edge overlay（opacity 0.08〜0.16） | 仕様上 optional。ROOF QA（法線可視化）で面の切り分けは確認できるため見送り |

---

**production / protected は変更していません。cutover もしていません。**
スクリーンショットをご確認のうえ、production へ反映するかご判断ください。

`HIGH_LOD_VISUAL_QUALITY_SUCCESS`
